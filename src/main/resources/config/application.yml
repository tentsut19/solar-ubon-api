info:
  app:
    name: ${project.name}
    description: ${project.description}
    version: ${project.version}
server:
  port: ${PORT:9801}

queue:
#  interval-cron: '0 0 1-2 * * *'
#  interval-cron: '*/10 * * * * *'
  interval-cron: '0 0 1-2 * * *'
  weather-cron: '0 0 3-4 * * *'
  weather-today-cron: '0 0 8-9 * * *'
  enable: true
